# Iterate over list and remove element one by one (.pop()) then printing it.
# until there are no elements left. Use for or while loop, whichever is appropriate.

a_list = [1, 2, 3, 4]
print(f"Starting with {a_list}")


print(f"Ended with {a_list}")
assert a_list == []
print("OK")
