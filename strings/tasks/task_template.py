# Write `a_template` that will use placeholders so that you can use .format(city=city, (and so on))
# print the result at the end
city = "Warsaw"
country = "Poland"
population = 1_864_035

a_template = ""

result = a_template.format()
