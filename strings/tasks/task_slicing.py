# Using slicing, create another string without ".com" suffix,
# then generate 5 different addresses using different domains,
# e.g. .pl. .info, .dev, .de, .co.uk
# print them all
a_string = "www.google.com"
