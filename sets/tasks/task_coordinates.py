# A couple of coordinates pairs are given.
# Transform them in such a way, that this information
# can be stored in a set for later checking e.g. if a given city was visited already.
# Add both transformed coordinates data into set, then try to add the same for the second time.
# print the set before and after - see what changes (if anything).

a_set = {}
coordinates_warsaw = [52.229, 21.008]
coordinates_lodz = [51,759, 19.456]
